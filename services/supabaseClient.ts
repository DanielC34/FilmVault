// The Supabase client is currently imported using a remote ESM URL which is not compatible with Next.js builds.
// Since Supabase is not currently used in the application, this module is disabled so it does not break the TypeScript build.
export const supabase = null;
